#include <stdio.h>
#include <stdlib.h>

// Operations on arrays
// Traversal - visiting every element of the array at least once
// Insertion - Inserting a value at a given index in the array
// Deletion - Deleting a value at a given index in the array
// Searching - Searching a value in the array and giving its index
//      if not sorted - linear search
//      if sorted - binary search

int main()
{

    return 0;
}